pluginManagement {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenLocal()
		mavenCentral()
		gradlePluginPortal()
	}
}
rootProject.name = 'CurieContent'
if (System.getenv("CI") != null) {
	includeBuild("../CurieAPI")
}
